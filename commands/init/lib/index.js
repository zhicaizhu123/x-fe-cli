'use strict';

module.exports = init

console.log()

/**
 * 初始化项目
 *
 * @param {*} projectName 项目名称
 * @param {*} options 命令参数
 */
function init(projectName, options) {
    console.log('init', projectName, options.force)
}